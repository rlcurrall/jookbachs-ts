{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "npm install && lerna exec npm install",
    "start": "env-cmd lerna run start",
    "build": "lerna run build",
    "watch": "env-cmd lerna run watch",
    "lint": "lerna run lint"
  },
  "devDependencies": {
    "lerna": "^3.18.3"
  },
  "dependencies": {
    "env-cmd": "^10.0.1",
    "jookbachs-server": "file:server",
    "www": "file:www"
  }
}
